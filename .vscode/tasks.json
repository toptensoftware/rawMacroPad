{
	"version": "2.0.0",
	"tasks": [
	{
		"label": "Build Project",
		"type": "shell",
 		"command": "bash",
		"args": [
			"-c",
			"./b"   // just run script in workspace folder
		],
		"options": {
			"cwd": "${workspaceFolder}"  // make sure Bash starts here
		},
		"problemMatcher": [],
		"group": {
			"kind": "build",
			"isDefault": true
		}
    }
	]
}